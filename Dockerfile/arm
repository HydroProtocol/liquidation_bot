# compile env
FROM golang:1.12

ENV CGO_ENABLED 0
ENV GOOS linux
ENV GOARCH arm
ENV GOARM 5

WORKDIR /app
COPY . /app
RUN go mod download
RUN go build -o main

FROM alpine
RUN apk --no-cache add ca-certificates
COPY --from=0 /app/main /bin/main